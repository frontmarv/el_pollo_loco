Playback control:

audio.play() - starts playing
audio.pause() - pauses (can resume from same position)
audio.load() - reloads the audio file
Position:

audio.currentTime = 0 - resets to beginning
audio.currentTime = 5 - jumps to 5 seconds
Volume:

audio.volume = 0.5 - set volume (0.0 to 1.0)
audio.muted = true - mute
audio.muted = false - unmute
Looping:

audio.loop = true - loops forever
audio.loop = false - plays once

Common patterns:

// Stop (pause and reset)
this.sounds.walking.pause();
this.sounds.walking.currentTime = 0;

// Loop
this.sounds.snoring.loop = true;
this.sounds.snoring.play();

// Mute all
this.sounds.walking.muted = true;

// Check if playing
!this.sounds.walking.paused

Properties to check:

audio.paused - true if paused/stopped
audio.ended - true if finished playing
audio.duration - total length in seconds